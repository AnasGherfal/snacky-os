name: Buying purchase and storage acceptance
on:
  push:
    branches: [feat/buying-list-purchase-receipt-v1]
  pull_request:
    paths:
      - 'src/lib/buying-*'
      - 'src/components/Buying*'
      - 'src/app/buying-lists/**'
      - 'src/app/api/buying-lists/**'
      - 'supabase/migrations/*buying_purchase_link*'
      - 'scripts/*buying-purchase*'
      - '.github/workflows/buying-purchase.yml'
permissions:
  contents: read
concurrency:
  group: buying-receipt-${{ github.ref }}
  cancel-in-progress: true
jobs:
  acceptance:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Exact receipt contracts and existing buying permissions
        run: |
          node --experimental-strip-types --test scripts/test-buying-purchase.mjs scripts/test-buying-sources.mjs scripts/test-shared-buying.mjs scripts/test-buying-print-css.mjs scripts/test-authz-permissions.mjs
          npm run typecheck
          npx eslint src/lib/buying-purchase.ts src/components/BuyingPurchaseClient.tsx src/app/api/buying-lists/purchases/route.ts 'src/app/buying-lists/[id]/purchases/page.tsx' 'src/app/buying-lists/[id]/page.tsx'
      - name: Existing route and purchase ledger protections
        run: |
          npm run test:critical-workflows
          npm run test:route-ledger-core
      - uses: supabase/setup-cli@v1
        with:
          version: 2.117.0
      - name: Disposable backend; no linked production project
        run: |
          mkdir -p .qa/full/supabase diagnostics
          python3 - <<'PY'
          from pathlib import Path
          text=Path('supabase/config.toml').read_text().replace('project_id = "snacky-os"','project_id = "snacky-company-isolated"')
          for section in ['db.migrations','db.seed','studio','inbucket','realtime','edge_runtime']:
            start=text.index('['+section+']');end=text.find('\n[',start+1)
            if end<0:end=len(text)
            text=text[:start]+text[start:end].replace('enabled = true','enabled = false')+text[end:]
          Path('.qa/full/supabase/config.toml').write_text(text)
          PY
          supabase start --workdir .qa/full > diagnostics/local-start.log 2>&1 || { tail -n 60 diagnostics/local-start.log; exit 1; }
          supabase status --workdir .qa/full -o env > .qa/local-status.env
          python3 scripts/company-e2e/migrate.py
      - name: Isolated browser tools
        run: |
          npm install --prefix .qa/browser --no-package-lock --ignore-scripts playwright@1.55.0 @axe-core/playwright@4.10.2
          .qa/browser/node_modules/.bin/playwright install --with-deps chromium
      - name: Real buying purchase, ledger and Arabic phone acceptance
        run: |
          set -o pipefail
          node --import ./scripts/company-e2e/diagnostics.mjs scripts/buying-purchase-e2e.mjs 2>&1 | tee diagnostics/buying-purchase-run.log
      - name: Redact isolated credentials
        if: always()
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          for p in Path('diagnostics').rglob('*.log'):
            text=p.read_text(errors='replace')
            text=re.sub(r'eyJ[A-Za-z0-9_\-.]+','[LOCAL_JWT_REDACTED]',text)
            text=re.sub(r'sb_(?:secret|publishable)_[A-Za-z0-9_-]+','[LOCAL_KEY_REDACTED]',text)
            p.write_text(text)
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: buying-purchase-acceptance
          path: diagnostics/
          retention-days: 7
      - name: Destroy isolated backend
        if: always()
        run: supabase stop --workdir .qa/full --no-backup || true
