name: Apply manual sale search fix

on:
  push:
    branches:
      - agent/fix-manual-sale-product-search

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-manual-sale-product-search
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-manual-sale-product-search-fix.mjs
      - run: |
          rm scripts/apply-manual-sale-product-search-fix.mjs
          rm .github/workflows/apply-manual-sale-search-fix.yml
          rm .github/manual-sale-search-trigger.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix manual sale full catalog search"
          git push origin HEAD:agent/fix-manual-sale-product-search
