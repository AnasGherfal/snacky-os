import { revalidatePath } from "next/cache";import { redirect } from "next/navigation";import { AppShell } from "@/components/AppShell";import { FormSection, PageHeader, PrimaryButton } from "@/components/ui";import { getSupabaseServerClient } from "@/lib/supabase-server";
async function create(fd:FormData){"use server";const s=getSupabaseServerClient();if(!s)return;await s.from("suppliers").insert({name:String(fd.get("name")||"").trim(),contact_name:String(fd.get("contact_name")||"")||null,phone:String(fd.get("phone")||"")||null,payment_terms:String(fd.get("payment_terms")||"")||null});revalidatePath('/suppliers');redirect('/suppliers');}
export default function Page(){return <AppShell><PageHeader title="New supplier"/><FormSection title="Supplier details"><form action={create} className="grid gap-3 md:grid-cols-2"><input required name="name" className="field-input" placeholder="Name"/><input name="contact_name" className="field-input" placeholder="Contact"/><input name="phone" className="field-input" placeholder="Phone"/><input name="payment_terms" className="field-input" placeholder="Payment terms"/><PrimaryButton>Add supplier</PrimaryButton></form></FormSection></AppShell>}
