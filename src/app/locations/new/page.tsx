import { revalidatePath } from "next/cache";import { redirect } from "next/navigation";import { AppShell } from "@/components/AppShell";import { FormSection, PageHeader, PrimaryButton } from "@/components/ui";import { getSupabaseServerClient } from "@/lib/supabase-server";
async function create(fd:FormData){"use server";const s=getSupabaseServerClient();if(!s)return;const p={name:String(fd.get("name")||"").trim(),location_type:String(fd.get("location_type")||"other"),rent_amount:Number(fd.get("rent_amount")||0),status:String(fd.get("status")||"active")};if(!p.name)return;await s.from("locations").insert(p);revalidatePath('/locations');redirect('/locations');}
export default function Page(){return <AppShell><PageHeader title="New location"/><FormSection title="Location details"><form action={create} className="grid gap-3 md:grid-cols-2"><input required name="name" className="field-input" placeholder="Location name"/><input required name="location_type" className="field-input" placeholder="school/hospital/..."/><input type="number" step="0.01" name="rent_amount" className="field-input" placeholder="Rent"/><input name="status" className="field-input" defaultValue="active"/><PrimaryButton>Add location</PrimaryButton></form></FormSection></AppShell>}
