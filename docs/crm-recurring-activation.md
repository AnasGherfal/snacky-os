# Recurring-work activation — 17 September 2026

The owner explicitly requested live installation and activation after PR #174 passed its code, database and real isolated browser checks.

## Installation order and controls

The reviewed migration `20260916214951_crm_recurring_management.sql` was installed before application activation. The additional `20260917134507_crm_scheduler_extension.sql` was generated by Supabase CLI 2.117.0 and installs only the scheduler extension. It does not create a job or task and is safe when pg_cron is already installed. The temporary migration-generation workflow was removed from the final tree.

This activation changes application visibility to default-on; `NEXT_PUBLIC_SNACKY_CRM_RECURRING_ENABLED=false` remains the emergency app rollback switch. This supersedes the earlier default-off description in the original rollout guide. Database generation and individual routines retain separate controls. A new routine still starts paused.

Deploy the matching application only after final checks pass. Configure only the named `snacky-crm-recurring` database job using the existing installation script while generation is paused. The schedule is every 15 minutes. Verify owner reads, non-management denial, exact job command and permissions, then explicitly enable generation. Retain tasks and occurrence history on rollback; do not drop the new schema or remove the shared pg_cron extension to roll back the interface.

When applying these reviewed files through the Management API, reconcile only the just-applied migration receipt with its CLI-generated repository version after confirming its name and SQL. Do not rerun an installed file or alter unrelated migration history.

## First operational rollout

Staff rollout remains distinct from publishing Company guidance. Existing draft policies must not be auto-published. Use actual active staff accounts; no placeholder account or invented email. Start with an owner-controlled review responsibility and a small documented onboarding queue, then assign location-specific schedules when the intended employee account and approved cadence are available. Do not change rent terms or financial records to create reminders.

The final PR deployment comment records the actual installed state, application commit, scheduler status and verification results. This file alone is not evidence of a completed deployment, a staff browser session or a verified backup restoration.
